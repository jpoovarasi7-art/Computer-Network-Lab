//---------------Counting Bits----------------
class Solution {
    public int[] countBits(int n) {
        int[] ans = new int[n+1];
        for(int i=0;i<=n;i++)
        {
            String s = Integer.toBinaryString(i);
            ans.append(Integer.parseInt(s));
        }
        return ans;
    }
}
